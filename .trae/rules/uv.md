python项目的环境、包管理、依赖安装等操作，全部采用uv管理器

项目中的测试框架采用pytest，所有的测试内容都在 tests目录下。并且要严格遵守pytest的命名规范。让VScode能够识别测试结构。
所有测试均使用 np.testing.assert_allclose 进行近似比较，符合数值计算的容差要求。
我的项目核心是为了学习位姿变换、相机标定、3D点云生成等技术，所以我想要构建自己的算法库。命名为 YZX_lib。
